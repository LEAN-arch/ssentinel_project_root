# sentinel_project_root/.env.example
# Copy this file to .env and fill in your environment-specific variables.
# The application will load these variables at startup.

# --- Mapbox Configuration ---
# A Mapbox token is required to render choropleth maps.
# Get a free token from https://www.mapbox.com/
SENTINEL_MAPBOX_TOKEN="pk.YOUR_MAPBOX_PUBLIC_ACCESS_TOKEN_HERE"

# --- Logging Configuration ---
# Overrides the default LOG_LEVEL from config/settings.py.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# SENTINEL_LOG_LEVEL="DEBUG"

# --- Database Configuration (Example for future use) ---
# SENTINEL_DATABASE_URL="postgresql://user:password@host:port/dbname"
